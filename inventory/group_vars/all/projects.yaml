---
controller_projects:
  - name: "00-{{ tenant_name }}-example-project"
    scm_type: git
    scm_url: "https://github.com/tosmi-ansible/{{ tenant_name }}-example-project.git"
    scm_branch: main
    scm_clean: true
    credentials: "00-{{ tenant_name }}-github"
    description: Example Project
    organization: "{{ tenant_name }}"
    wait: true
    update_project: true
